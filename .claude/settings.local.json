{
  "permissions": {
    "allow": [
      "WebSearch",
      "WebFetch(domain:ktor.io)"
    ],
    "deny": [
      "Read(server/.env)",
      "Edit(server/.env)",
      "Read(tv-app/.env)",
      "Edit(tv-app/.env)"
    ]
  }
}
